/**
 * Tokens barrel — re-exports every design token from the UI package.
 *
 * @example
 *   import { colors, typography, spacing, borderRadius } from "@worktf/ui/tokens";
 */

export { colors } from "./colors";
export { typography } from "./typography";
export { spacing, borderRadius } from "./spacing";
