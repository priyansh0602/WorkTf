/**
 * @worktf/shared — Package entry point.
 *
 * Re-exports all types, enums, and utility functions so consumers
 * can import from "@worktf/shared" with a single path.
 *
 * @example
 *   import { ICall, CallStatus, formatDuration } from "@worktf/shared";
 */

export * from "./types";
export * from "./utils";
